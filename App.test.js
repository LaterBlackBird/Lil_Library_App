import * as React from 'react';
import { render, screen, fireEvent } from '@testing-library/react-native';

describe("Login Form", () => {
  test('should take a user to the sign up form when signup button is clicked', () => {  })
})